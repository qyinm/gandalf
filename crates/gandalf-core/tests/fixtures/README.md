# gandalf-core test fixtures

Shared JSON and sandbox directories for parity tests against `packages/core/tests/*`.

- `evidence/` — serialized `DiscoveredItem` samples
- `sandboxes/` — minimal home/project trees for scan and restore tests
- `parity/` — expected outputs copied from TypeScript characterization tests