# snaptailor Deferred Scope

## v0.2+ Candidates

- Restore planner and `restore --dry-run`.
- `restore --apply` with per-risk confirmation and rollback plan.
- Import/export `.stailor` bundles.
- Bundle quarantine and path traversal hardening.
- Signed bundles and provenance verification.
- Team sharing.
- Desktop UI.
- Cloud sync.
- Cross-OS restore.
- Raw content capture with explicit user opt-in.

## Validation Work

- Collect 10 real agent-behavior drift incidents.
- Manually reconstruct which files/settings caused each incident.
- Classify each incident as captured, redacted, remote, unsupported, or unknown.
- Use results to decide whether Codex/Cursor deep scanners should move into v0.1 or v0.2.

