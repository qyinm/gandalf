{
  "name": "hem-workspace",
  "version": "0.3.3",
  "private": true,
  "description": "Workspace for Hem CLI, TUI, core, and desktop apps.",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd apps/tui build && bun run --cwd apps/cli build && bun run --cwd apps/desktop build",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd apps/tui typecheck && bun run --cwd apps/cli typecheck && bun run --cwd apps/desktop typecheck",
    "test": "bun run --cwd packages/core test && bun run --cwd apps/tui test && bun run --cwd apps/cli test && bun run --cwd apps/desktop test",
    "check": "bun run build && bun run test",
    "desktop:dev": "bun run --cwd apps/desktop tauri dev",
    "desktop:build": "bun run --cwd apps/desktop tauri build",
    "prepublishOnly": "bun run check",
    "dogfood:cross-machine": "bun scripts/cross-machine-dogfood.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@types/react": "^19.2.15",
    "typescript": "^5.8.3"
  }
}
