{
  "name": "gandalf-workspace",
  "version": "0.4.1",
  "private": true,
  "description": "Workspace for Gandalf landing app and Go CLI tooling.",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --cwd apps/landing build",
    "typecheck": "bun run --cwd apps/landing typecheck",
    "test": "bun run go:test",
    "check": "bun run typecheck && bun run build && bun run test",
    "go:test": "go test ./...",
    "go:build": "go build -o bin/gandalf ./cmd/gandalf",
    "install:smoke": "./scripts/install-smoke.sh",
    "dogfood:cross-machine": "bun scripts/cross-machine-dogfood.mjs",
    "dogfood:gate2": "bun scripts/gate2-acceptance.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@types/react": "^19.2.15",
    "typescript": "^5.8.3"
  }
}
