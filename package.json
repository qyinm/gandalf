{
  "name": "gandalf",
  "private": true,
  "version": "0.7.0",
  "description": "Infrastructure as Code for AI Agents",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build && bun run build:go",
    "build:web": "turbo run build",
    "build:go": "mkdir -p bin && go build -o bin/gandalf ./cmd/gandalf",
    "test": "bun run test:go && turbo run test",
    "test:go": "go test -count=1 ./...",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules apps/*/node_modules apps/*/dist apps/*/.turbo .turbo"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "bun@1.2.0"
}
