<section class="pledge">
  <div class="container">
    <div class="pledge__inner">
      <svg class="pledge__icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      <p class="pledge__text">
        By design, Hem can inspect your setup without executing MCP commands, scripts, or plugins.
        It never phones home. It never alters your configuration without explicit consent. It stores metadata-first and
        omits raw secrets. <em>Your agent setup should be safe to experiment with and easy to restore.</em>
      </p>
    </div>
  </div>
</section>

<style>
  .pledge {
    padding: 0 0 60px;
  }

  .pledge__inner {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    max-width: 640px;
    margin: 0 auto;
    padding: 18px 22px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-sm);
  }

  .pledge__icon {
    flex-shrink: 0;
    color: var(--brand);
    margin-top: 2px;
  }

  .pledge__text {
    font-size: 0.85rem;
    color: var(--muted-foreground);
    line-height: 1.7;
  }

  .pledge__text em {
    color: var(--brand);
    font-style: italic;
  }

  @media (max-width: 480px) {
    .pledge__inner {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
  }
</style>