---
title: "Welcome to Gandalf"
description: "Agent Environment as Code — Standardize, verify, and synchronize AI agent setups across engineering teams."
---

# Welcome to Gandalf

Gandalf is the local control console and standardizer for AI agent environments (Claude Code, OpenAI Codex).

Just as **Docker** solved *"it works on my machine"* for runtime containers, **Gandalf** solves *"it works on my agent"* for AI-assisted engineering teams.

```
                    ┌─────────────────────────┐
                    │      gandalf.toml       │
                    │ (MCPs, Skills, Hooks)   │
                    └────────────┬────────────┘
                                 │
                         gandalf apply
                                 │
            ┌────────────────────┴────────────────────┐
            ▼                                         ▼
   Claude Code (~/.claude)                   OpenAI Codex (~/.codex)
   ├── Postgres DB MCP                       ├── Postgres DB MCP
   ├── Team PR Review Skill                  ├── Team PR Review Skill
   └── Pre-save Lint Hook                    └── Pre-save Lint Hook
```

---

## Why Gandalf?

Modern software teams develop primarily using AI agents. Previously, onboarding a developer meant installing an IDE and a few extensions. Today, team members need:

1. **Shared MCP Servers** (staging database connectors, internal API search, log query tools)
2. **Standardized Skills** (team coding guidelines, PR reviewer skills, prompt recipes)
3. **Guardrail Hooks** (lint before save, sensitive file protection)

Without a unified declarative setup, each developer's agent has different capabilities, missing MCP tools, or conflicting configurations.

Gandalf provides **Agent Environment as Code**: declare everything in a single `gandalf.toml` in your repository root, verify drift with `gandalf check`, and synchronize local setups with `gandalf apply`.

---

## Key Capabilities

<CardGroup cols={2}>
  <Card title="Agent Environment as Code" icon="file-code">
    Declare MCP servers, skills, and hooks in a Git-tracked `gandalf.toml`.
  </Card>
  <Card title="Non-Destructive Smart Merge" icon="shield-check">
    Safely injects team tools without deleting or modifying personal settings.
  </Card>
  <Card title="Drift Detection & CI" icon="radar">
    Detect missing agent capabilities automatically locally or in CI pipelines.
  </Card>
  <Card title="Instant Safety Rollback" icon="rotate-left">
    Pre-apply backups allow one-click rollback with `gandalf restore`.
  </Card>
</CardGroup>

---

## Next Steps

<Card title="Quickstart Guide" icon="bolt" href="/docs/quickstart">
  Install Gandalf and synchronize your team agent setup in under 3 minutes.
</Card>
